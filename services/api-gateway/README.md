# API Gateway

This service is the single entry point for all external clients, such as the Web Dashboard and TUI. It exposes a RESTful API based on the OpenAPI specification, validates incoming requests, and routes them to the appropriate backend services.
